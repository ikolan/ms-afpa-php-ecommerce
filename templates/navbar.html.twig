<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Nom du site</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link" href="{{ path('products') }}">Produits</a>
        </li>
      </ul>
      <div class="d-flex">
        {% if ( app.user == null ) %}
            <a href="{{ path('cart') }}" class="btn btn-warning ms-3"><i class="fas fa-shopping-cart"></i></a>
            <a href="{{ path('auth') }}" class="btn btn-primary ms-3"><i class="fas fa-user"></i></a>
        {% else %}
            <a href="{{ path('user') }}" class="btn btn-primary"><i class="fas fa-user"></i> {{ app.user.firstName }} {{ app.user.lastName }}</a>
            <a href="{{ path('cart') }}" class="btn btn-warning ms-3"><i class="fas fa-shopping-cart"></i></a>
            <a href="{{ path('auth_logout') }}" class="btn btn-danger ms-3"><i class="fas fa-sign-out-alt"></i></a>
            {% if "ROLE_ADMIN" in app.user.roles %}
              <a href="{{ path('admin') }}" class="btn btn-secondary ms-3"><i class="fas fa-tachometer-alt"></i></a>
            {% endif %}
        {% endif %}
        </div>
    </div>
  </div>
</nav>